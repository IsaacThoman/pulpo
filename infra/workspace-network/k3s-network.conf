[Unit]
Wants=pulpo-workspace-network.service
After=pulpo-workspace-network.service

[Service]
# Reapply on every K3s start; a failed load prevents K3s from starting.
ExecStartPre=/usr/sbin/nft -f /etc/pulpo/workspace-network/rules.nft
