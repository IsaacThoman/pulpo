{
  "name": "ocr-middleware",
  "version": "1.0.0",
  "description": "LLM proxy with OCR interception, admin UI, and Postgres persistence.",
  "scripts": {
    "frontend:build": "npm --prefix frontend run build",
    "frontend:start": "npm --prefix frontend run start -- --host 0.0.0.0 --port 4200",
    "prisma:generate": "npx prisma generate",
    "prisma:migrate": "npx prisma migrate dev",
    "prisma:deploy": "npx prisma migrate deploy",
    "test:e2e": "npx playwright test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/IsaacThoman/ocr-middleware.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "bugs": {
    "url": "https://github.com/IsaacThoman/ocr-middleware/issues"
  },
  "homepage": "https://github.com/IsaacThoman/ocr-middleware#readme",
  "dependencies": {
    "@prisma/client": "6.19.2",
    "js-tiktoken": "^1.0.21",
    "openai": "^6.29.0",
    "prisma": "6.19.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
