export default function Index() {
  // RootLayout renders the native Mockup5 navigator for `/`. Keep this route
  // anchor so Expo Router always has a concrete match for the app root.
  return null
}
